[tools]
node = "24.20.0"
nub = "0.8.0"

[tasks.setup]
description = "Install locked dependencies and browser binaries for Playwright"
run = ["nub ci", "nubx playwright install chromium webkit"]

[tasks.check]
description = "Run the complete repository validation suite"
run = "nub run check"
